{
  "agent": "security-compliance",
  "case": "negative-slopsquat-and-mutable-slug",
  "polarity": "negative",
  "enforcement": "ci-job",
  "driver": "pin-check",
  "title": "The risky slug-helper PR (uuidd typo of uuid + slugify:latest + a git-source dep, no lockfile) → security gate RED with a DIFFERENT dep set than S1, proving pin-check is content-general",
  "input": {
    "manifest": "demos/scenarios/s3-risky-dependency/fixtures/security/bad-slug-deps-package.json"
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["slopsquat", "mutable"]
  }
}
