{
  "name": "synthetic-unpinned-slug-fixture",
  "version": "0.0.0",
  "private": true,
  "_comment": "NEGATIVE data for S3 — legitimately-named slug deps (no typosquat) but caret ranges with NO committed lockfile, so they are not reproducibly pinned. Proves the gate bites on pinning hygiene alone, independent of slopsquatting. pin-check must fire no-lockfile + unpinned.",
  "dependencies": {
    "nanoid": "^5.0.7",
    "slugify": "^1.6.6"
  }
}
