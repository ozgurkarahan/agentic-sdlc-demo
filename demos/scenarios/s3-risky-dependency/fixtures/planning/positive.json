{
  "agent": "planning",
  "case": "positive-risky-dep-plan-ordered",
  "polarity": "positive",
  "enforcement": "local-assertion",
  "driver": "plan-lint",
  "title": "Planning stays content-general on a security-feature intent: the slug-helper plan emits a sound DAG (integration test depends on the helper + wiring units, marked ordered) — plan-lint has no dependency-risk or rate-limit coupling",
  "input": {
    "intent": "Add a slug-collision helper library so generated slugs don't clash.",
    "plan": {
      "intent": "Add a slug-collision helper library so generated slugs don't clash.",
      "planApproved": true,
      "units": [
        { "id": "U1", "title": "add pinned slug helper dependency + lockfile", "paths": ["demos/sample-app/package.json", "demos/sample-app/package-lock.json"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U2", "title": "wire collision-check into the create handler", "paths": ["demos/sample-app/src/app.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U3", "title": "docs — note the new dependency + rationale", "paths": ["demos/sample-app/README.md"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U4", "title": "integration test (forces a collision; asserts a unique slug is regenerated)", "paths": ["demos/sample-app/test/e2e/slugCollision.e2e.test.ts"], "parallelSafe": false, "dependsOn": ["U1", "U2"] }
      ]
    }
  },
  "expect": {
    "outcome": "pass",
    "signals": []
  }
}
