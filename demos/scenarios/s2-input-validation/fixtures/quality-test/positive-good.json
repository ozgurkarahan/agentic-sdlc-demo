{
  "agent": "quality-test",
  "case": "positive-good-validation",
  "polarity": "positive",
  "enforcement": "ci-job",
  "driver": "eval-rubric",
  "title": "A correct max-length guard (400 + JSON error on over-long, 201 on a normal url) scores full marks → evals green",
  "input": { "appVariant": "good" },
  "expect": { "outcome": "pass", "signals": [] }
}
