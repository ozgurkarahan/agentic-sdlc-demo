{
  "agent": "quality-test",
  "case": "negative-plaintext-error",
  "polarity": "negative",
  "enforcement": "ci-job",
  "driver": "eval-rubric",
  "title": "An impl that rejects with a non-JSON 400 (right status, wrong contract) must STILL turn the evals gate RED — the oracle checks response shape, not just the code",
  "input": { "appVariant": "plaintext-error" },
  "expect": { "outcome": "blocked", "signals": ["overlong-error-not-json"] }
}
