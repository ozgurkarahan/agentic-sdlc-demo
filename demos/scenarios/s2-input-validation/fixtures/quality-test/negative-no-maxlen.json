{
  "agent": "quality-test",
  "case": "negative-no-maxlen",
  "polarity": "negative",
  "enforcement": "ci-job",
  "driver": "eval-rubric",
  "title": "An impl that stores over-long URLs (no length guard at all) limits nothing and must turn the evals gate RED",
  "input": { "appVariant": "no-maxlen" },
  "expect": { "outcome": "blocked", "signals": ["overlong-url-not-rejected"] }
}
