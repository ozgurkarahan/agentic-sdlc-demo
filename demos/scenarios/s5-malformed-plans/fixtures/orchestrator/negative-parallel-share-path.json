{
  "agent": "orchestrator",
  "case": "negative-parallel-units-share-path",
  "polarity": "negative",
  "enforcement": "local-assertion",
  "driver": "plan-lint",
  "title": "Two units both marked parallelSafe:true that claim the same owned path are a hidden cross-unit dependency (a write-write conflict if run concurrently). plan-lint rule B must catch it",
  "input": {
    "plan": {
      "intent": "Add a slug-collision guard (with two parallel units fighting over one file).",
      "planApproved": false,
      "units": [
        { "id": "U1", "title": "collision-guard helper", "paths": ["demos/sample-app/src/lib/slug.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U2", "title": "validation tweak in the SAME helper file", "paths": ["demos/sample-app/src/lib/slug.ts"], "parallelSafe": true, "dependsOn": [] }
      ]
    }
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["parallel-units-share-path"]
  }
}
