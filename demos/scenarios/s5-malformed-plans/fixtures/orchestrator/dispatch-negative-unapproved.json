{
  "agent": "orchestrator",
  "case": "dispatch-negative-unapproved-refused",
  "polarity": "negative",
  "enforcement": "local-assertion",
  "driver": "dispatch",
  "title": "Even a perfectly-formed plan is refused fan-out while the human plan-approved label is absent — the dispatcher chooses not to dispatch (🟦 orchestration gate, NOT native enforcement)",
  "input": {
    "planApprovedLabel": false,
    "units": [
      { "id": "U1", "title": "collision-guard helper", "paths": ["demos/sample-app/src/lib/slug.ts"], "parallelSafe": true, "dependsOn": [] },
      { "id": "U2", "title": "config surface", "paths": ["demos/sample-app/src/config.ts"], "parallelSafe": true, "dependsOn": [] }
    ]
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["refused-unapproved"]
  }
}
