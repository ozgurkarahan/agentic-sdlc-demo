{
  "agent": "orchestrator",
  "case": "positive-sound-dag-passes-plan-lint",
  "polarity": "positive",
  "enforcement": "local-assertion",
  "driver": "plan-lint",
  "title": "A well-formed plan (unique ids, acyclic DAG, parallel-safe units own distinct paths, the integration unit is correctly ordered) passes plan-lint clean — no structural or parallelization violation",
  "input": {
    "plan": {
      "intent": "Add a slug-collision guard to the URL-shortener.",
      "planApproved": true,
      "units": [
        { "id": "U1", "title": "collision-guard helper", "paths": ["demos/sample-app/src/lib/slug.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U2", "title": "config surface", "paths": ["demos/sample-app/src/config.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U3", "title": "docs section", "paths": ["demos/sample-app/README.md"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U4", "title": "integration test (drives the collision path)", "paths": ["demos/sample-app/test/e2e/slug.e2e.test.ts"], "parallelSafe": false, "dependsOn": ["U1", "U2"] }
      ]
    }
  },
  "expect": {
    "outcome": "pass",
    "signals": []
  }
}
