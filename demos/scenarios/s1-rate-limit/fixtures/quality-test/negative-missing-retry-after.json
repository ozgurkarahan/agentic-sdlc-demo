{
  "agent": "quality-test",
  "case": "negative-missing-retry-after",
  "polarity": "negative",
  "enforcement": "ci-job",
  "driver": "eval-rubric",
  "title": "An impl that returns 429 but omits Retry-After fails the full output contract → evals gate RED",
  "input": {
    "appVariant": "missing-retry-after"
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["missing-retry-after"]
  }
}
