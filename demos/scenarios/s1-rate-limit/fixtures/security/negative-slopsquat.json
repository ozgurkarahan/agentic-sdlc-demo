{
  "agent": "security-compliance",
  "case": "negative-slopsquat-and-mutable",
  "polarity": "negative",
  "enforcement": "ci-job",
  "driver": "pin-check",
  "title": "A synthetic manifest (DECOUPLED from the rate-limit story) with hallucinated deps (expresss/axioss) + mutable specs (left-pad:* , evil-utils:latest) and no lockfile → security gate RED",
  "input": {
    "manifest": "demos/scenarios/s1-rate-limit/fixtures/security/bad-deps-package.json",
    "sink": "child_process.exec(req.query.cmd) — modelled as a CodeQL finding (native GHAS) in T2, not executed in T1"
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["slopsquat", "mutable"]
  }
}
