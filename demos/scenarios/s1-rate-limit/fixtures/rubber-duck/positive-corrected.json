{
  "agent": "rubber-duck",
  "case": "positive-corrected-plan",
  "polarity": "positive",
  "enforcement": "local-assertion",
  "driver": "plan-lint",
  "title": "Rubber-Duck re-reviews the corrected plan — no hidden dependency, no unsafe parallelization → PASS",
  "input": {
    "plan": {
      "intent": "Add rate limiting to the URL-shortener API so a single client can't exhaust the service.",
      "planApproved": true,
      "units": [
        { "id": "U1", "title": "limiter middleware", "paths": ["demos/sample-app/src/middleware/rateLimit.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U2", "title": "config surface", "paths": ["demos/sample-app/src/config.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U3", "title": "docs — rate-limit section", "paths": ["demos/sample-app/README.md"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U4", "title": "integration test", "paths": ["demos/sample-app/test/e2e/rateLimit.e2e.test.ts"], "parallelSafe": false, "dependsOn": ["U1", "U2"] }
      ]
    }
  },
  "expect": {
    "outcome": "pass",
    "signals": []
  }
}
