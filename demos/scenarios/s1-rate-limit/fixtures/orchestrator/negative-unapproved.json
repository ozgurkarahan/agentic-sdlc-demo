{
  "agent": "orchestrator",
  "case": "negative-unapproved-no-dispatch",
  "polarity": "negative",
  "enforcement": "local-assertion",
  "driver": "dispatch",
  "title": "An unapproved plan (no human plan-approved label) → dispatcher refuses to fan out any work (the gate is our orchestration choosing not to dispatch, NOT a GitHub primitive)",
  "input": {
    "planApprovedLabel": false,
    "units": [
      { "id": "U1", "title": "limiter middleware", "paths": ["demos/sample-app/src/middleware/rateLimit.ts"], "parallelSafe": true, "dependsOn": [] },
      { "id": "U2", "title": "config surface", "paths": ["demos/sample-app/src/config.ts"], "parallelSafe": true, "dependsOn": [] }
    ]
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["refused-unapproved"]
  }
}
