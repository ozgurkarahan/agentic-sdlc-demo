{
  "name": "synthetic-bad-deps-fixture",
  "version": "0.0.0",
  "private": true,
  "_comment": "DECOUPLED Security fixture (NOT the rate-limit app). A synthetic manifest with no sibling lockfile so pin-check fires no-lockfile + slopsquat + mutable. expresss/axioss are Levenshtein-1 typos of express/axios; left-pad:* and evil-utils:latest are mutable specs.",
  "dependencies": {
    "express": "^4.19.2",
    "expresss": "^4.18.0",
    "axioss": "^1.6.0",
    "left-pad": "*",
    "evil-utils": "latest"
  }
}
