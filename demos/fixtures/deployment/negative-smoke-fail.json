{
  "agent": "deployment",
  "case": "negative-smoke-fail-rollback",
  "polarity": "negative",
  "enforcement": "local-assertion",
  "driver": "smoke",
  "title": "A bad build fails the /healthz smoke probe → no-go + rollback fires (proves the deploy gate's rollback logic is real)",
  "input": {
    "smoke": "fail",
    "variant": "break-healthz.mjs"
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["rollback", "no-go"]
  }
}
