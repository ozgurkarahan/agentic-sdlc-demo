{
  "agent": "quality-test",
  "case": "negative-no-429",
  "polarity": "negative",
  "enforcement": "ci-job",
  "driver": "eval-rubric",
  "title": "An impl that counts but never returns 429 (passes unit tests, limits nothing) must turn the evals gate RED",
  "input": {
    "appVariant": "no-429"
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["no-429"]
  }
}
