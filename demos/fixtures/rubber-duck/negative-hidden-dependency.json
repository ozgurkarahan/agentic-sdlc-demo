{
  "agent": "rubber-duck",
  "case": "negative-hidden-dependency-and-unsafe-parallelization",
  "polarity": "negative",
  "enforcement": "local-assertion",
  "driver": "plan-lint",
  "title": "Rubber-Duck is handed a flawed plan: two 'parallel-safe' units share a limiter store (hidden cross-unit dependency) AND the integration test is mislabeled parallel-safe — both must be caught",
  "input": {
    "plan": {
      "intent": "Add rate limiting to the URL-shortener API so a single client can't exhaust the service.",
      "planApproved": false,
      "units": [
        { "id": "U1", "title": "limiter middleware", "paths": ["demos/sample-app/src/middleware/rateLimit.ts", "demos/sample-app/src/store/counter.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U2", "title": "config surface (also writes the shared counter store)", "paths": ["demos/sample-app/src/config.ts", "demos/sample-app/src/store/counter.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U3", "title": "docs — rate-limit section", "paths": ["demos/sample-app/README.md"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U4", "title": "integration test (drives past the limit)", "paths": ["demos/sample-app/test/e2e/rateLimit.e2e.test.ts"], "parallelSafe": true, "dependsOn": [] }
      ]
    }
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["parallel-units-share-path", "integration-marked-parallel"]
  }
}
