{
  "agent": "planning",
  "case": "negative-ordered-unit-marked-parallel",
  "polarity": "negative",
  "enforcement": "local-assertion",
  "driver": "plan-lint",
  "title": "Planning is given an intent with inherent ordering but marks the dependent unit parallel-safe — the artifact-contract check must catch it",
  "input": {
    "intent": "Migrate the limiter's counter store to Redis, then switch the limiter middleware to read from it.",
    "plan": {
      "intent": "Migrate the limiter's counter store to Redis, then switch the limiter middleware to read from it.",
      "planApproved": true,
      "units": [
        { "id": "U1", "title": "provision Redis-backed counter store", "paths": ["demos/sample-app/src/store/redisStore.ts"], "parallelSafe": true, "dependsOn": [] },
        { "id": "U2", "title": "switch limiter to read from the Redis store", "paths": ["demos/sample-app/src/middleware/rateLimit.ts"], "parallelSafe": true, "dependsOn": ["U1"] }
      ]
    }
  },
  "expect": {
    "outcome": "blocked",
    "signals": ["ordered-unit-marked-parallel"]
  }
}
